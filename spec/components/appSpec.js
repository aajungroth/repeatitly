/*
show own decks toggling:
  toggling show hide or show personal decks

show public decks togggling:
  should show or hide public decks

logout click:
  should take user to login page

create new deck button:
  should take user to create page

public deck:
  should only have study button visible

private, own deck:
  should have study, edit, and delete buttons visible

study button click:
  should load study component

edit button click:
  should load edit deck component

delete button click:
  should alert user to ask if they're sure

delete button click, yes im sure:
  should remove deck from table & localstorage

delete button and cancel:
  should not remove deck from table or localStorage
 */